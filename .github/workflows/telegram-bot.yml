# Disabled: GitHub cannot process Telegram button taps instantly (5+ min poll delay).
# Use the Telegram Web App button in reminders instead — instant, runs on GitHub Pages.
name: Telegram Bot Poll (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is intentionally disabled."
          echo "Log via the Open Tracker Web App button in Telegram reminders."
