{
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1",
  "devDependencies": {
  },
    "node-cron": "^3.0.3",
    "body-parser": "^1.20.2",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
  "dependencies": {
  "license": "MIT",
  "author": "",
  "keywords": ["tracker", "notes", "habits"]
  },
    "heroku-postbuild": "npm run install-client && npm run build",
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "start": "node server.js",
  "scripts": {
  "main": "server.js",
  "description": "Personal Tracker with Notes, Labels, and Daily Activity Tracking",
  "version": "1.0.0",
  "name": "personal-tracker"
  }
}

